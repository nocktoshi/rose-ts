// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: nockchain/common/v1/pagination.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nockchain.common.v1";

/**
 * Generic pagination parameters for list-style RPCs.
 * These types are intended to be reused across public APIs.
 *
 * Contract:
 * - The server may return fewer items than requested (client_page_items_limit is a hint).
 * - page_token is an opaque cursor produced by the server; clients must treat
 *   it as a black box. Servers may encode snapshot identity and last-key.
 * - For consistent pagination, clients should include the returned page_token
 *   in the next request without modification.
 * - Servers may enforce a maximum client_page_items_limit and/or byte budget regardless of
 *   client hints.
 */
export interface PageRequest {
  /**
   * Maximum number of items to return. The server may return fewer items
   * than requested. Clients should not rely on receiving exactly this count.
   */
  client_page_items_limit: number;
  /**
   * Opaque cursor returned by a previous call. When set, the server resumes
   * the listing from the position described by the token.
   * An empty token indicates the first page.
   */
  page_token: string;
  /**
   * Optional soft limit on the uncompressed bytes to return in a single page.
   * The server may ignore or cap this value according to policy. This refers
   * to the gRPC payload size after protobuf encoding and decompression.
   */
  max_bytes: string;
}

export interface PageResponse {
  /**
   * Opaque cursor for fetching the next page. Empty when there are no more
   * results.
   */
  next_page_token: string;
}

function createBasePageRequest(): PageRequest {
  return { client_page_items_limit: 0, page_token: "", max_bytes: "0" };
}

export const PageRequest: MessageFns<PageRequest> = {
  encode(message: PageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.client_page_items_limit !== 0) {
      writer.uint32(8).uint32(message.client_page_items_limit);
    }
    if (message.page_token !== "") {
      writer.uint32(18).string(message.page_token);
    }
    if (message.max_bytes !== "0") {
      writer.uint32(24).uint64(message.max_bytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.client_page_items_limit = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page_token = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.max_bytes = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PageRequest {
    return {
      client_page_items_limit: isSet(object.clientPageItemsLimit)
        ? globalThis.Number(object.clientPageItemsLimit)
        : isSet(object.client_page_items_limit)
        ? globalThis.Number(object.client_page_items_limit)
        : 0,
      page_token: isSet(object.pageToken)
        ? globalThis.String(object.pageToken)
        : isSet(object.page_token)
        ? globalThis.String(object.page_token)
        : "",
      max_bytes: isSet(object.maxBytes)
        ? globalThis.String(object.maxBytes)
        : isSet(object.max_bytes)
        ? globalThis.String(object.max_bytes)
        : "0",
    };
  },

  toJSON(message: PageRequest): unknown {
    const obj: any = {};
    if (message.client_page_items_limit !== 0) {
      obj.clientPageItemsLimit = Math.round(message.client_page_items_limit);
    }
    if (message.page_token !== "") {
      obj.pageToken = message.page_token;
    }
    if (message.max_bytes !== "0") {
      obj.maxBytes = message.max_bytes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageRequest>, I>>(base?: I): PageRequest {
    return PageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageRequest>, I>>(object: I): PageRequest {
    const message = createBasePageRequest();
    message.client_page_items_limit = object.client_page_items_limit ?? 0;
    message.page_token = object.page_token ?? "";
    message.max_bytes = object.max_bytes ?? "0";
    return message;
  },
};

function createBasePageResponse(): PageResponse {
  return { next_page_token: "" };
}

export const PageResponse: MessageFns<PageResponse> = {
  encode(message: PageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.next_page_token !== "") {
      writer.uint32(10).string(message.next_page_token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.next_page_token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PageResponse {
    return {
      next_page_token: isSet(object.nextPageToken)
        ? globalThis.String(object.nextPageToken)
        : isSet(object.next_page_token)
        ? globalThis.String(object.next_page_token)
        : "",
    };
  },

  toJSON(message: PageResponse): unknown {
    const obj: any = {};
    if (message.next_page_token !== "") {
      obj.nextPageToken = message.next_page_token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PageResponse>, I>>(base?: I): PageResponse {
    return PageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PageResponse>, I>>(object: I): PageResponse {
    const message = createBasePageResponse();
    message.next_page_token = object.next_page_token ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
